<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <LinearLayout
        android:id="@+id/view_story_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="bottom|center"
        android:background="#80000000"
        android:gravity="bottom|center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent">
<!--
        <ImageButton
            android:id="@+id/downloadImage"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="@dimen/preview_img_fab_margin"
            android:layout_marginEnd="@dimen/preview_img_fab_margin"
            android:background="@drawable/story_overlay_bottom_bar"
            android:contentDescription="@string/emptyText"
            app:srcCompat="@drawable/fab_download" />

        <ImageButton
            android:id="@+id/shareImage"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="@dimen/preview_img_fab_margin"
            android:layout_marginEnd="@dimen/preview_img_fab_margin"
            android:background="@drawable/story_overlay_bottom_bar"
            android:contentDescription="@string/emptyText"
            app:srcCompat="@drawable/fab_share" />
        -->
    </LinearLayout>
</merge>